# AI 内容创作工具 - 功能升级需求文档

**项目名称**: ai-content-txt (AI小说创作三阶段七循环系统)  
**当前使用模型**: volcengine-plan/ark-code-latest  
**文档版本**: v2.0  
**创建日期**: 2026-04-08

---

## 一、现有功能概述

### 1.1 当前核心功能

| 功能模块 | 描述 |
|---------|------|
| 三阶段生成 | 阶段1(骨架) → 阶段2(血肉) → 阶段3(打磨)，共7次循环 |
| 七大创作参数 | 读者定位、人物深度、情节架构、节奏掌控、感官细节、情感共鸣、反AI化 |
| 双模型支持 | 本地Ollama / 在线API自由切换 |
| 历史管理 | 自动保存创作历史，支持查看和复用 |
| 暗黑模式 | 支持浅色/暗黑/自动三种模式 |

### 1.2 现有技术栈

- React 18 + TypeScript
- Vite
- Tailwind CSS
- Zustand (状态管理)
- Lucide React (图标)

---

## 二、升级功能需求详情

### 2.1 全书生成功能 (核心升级)

#### 需求描述
**当前状态**: 只生成开篇第一章内容（约1500-100000字）  
**目标状态**: 生成完整的一本书，支持多章节结构和长篇内容

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F1.1 | 整本书按章节划分，支持指定章节数量（建议10-50章）或自动规划 | P0 |
| F1.2 | 每章独立生成逻辑，章与章之间有情节连贯性 | P0 |
| F1.3 | 支持设定每章目标字数（建议2000-5000字/章） | P0 |
| F1.4 | 生成全书大纲（目录结构），确认后开始分章生成 | P1 |
| F1.5 | 支持查看/编辑已生成的单章内容 | P1 |

#### 实现方案

```
全书生成流程:
1. 用户输入期望: 主题、关键词、期望字数、章节数
2. 生成阶段0: 整本书大纲（包含章节目录、每章标题一句话概括）
3. 用户确认大纲后可调整
4. 循环生成各章内容:
   - 每章 = 完整的三阶段七循环
   - 章节之间自动承接上下文
5. 最后一章完成后生成全书整合
```

---

### 2.2 模型输出限制处理

#### 需求描述
**问题**: 模型单次输出有token限制（约4K-8K tokens），长章节会超出限制  
**目标**: 智能分段生成，确保内容完整

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F2.1 | 模型输出字数监测，超出阈值自动截断并标记 | P0 |
| F2.2 | 剩余内容自动进入下一轮生成（续写模式） | P0 |
| F2.3 | 支持设置单次最大输出token数（默认4096） | P1 |
| F2.4 | 章节内分场景/分段生成，避免内容断裂 | P1 |
| F2.5 | 生成进度可视化，显示当前章节/段落进度 | P1 |

#### 实现逻辑

```
if (预估输出 > maxTokens):
    # 计算安全截断点（句子完整处）
    截断内容 = 找到最后完整句子
    剩余内容 = 截断后的内容
    
    # 保存已生成内容
    当前章节内容 += 截断内容
    
    # 自动续写
    prompt = 续写提示 + 剩余内容开头
    递归调用生成剩余部分
    
    # 合并内容
    当前章节内容 += 续写结果
```

---

### 2.3 修改优化功能

#### 需求描述
**目标**: 成品生成后，支持用户指定优化点，调用AI进行局部修改

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F3.1 | 最终成品展示区增加"优化修改"按钮 | P0 |
| F3.2 | 点击按钮弹出输入框，支持多行输入优化要求 | P0 |
| F3.3 | 优化调用使用阶段三的模型配置 | P0 |
| F3.4 | 优化请求支持指定优化范围（全文/指定章节/指定段落） | P1 |
| F3.5 | 优化历史记录，支持撤销本次优化 | P1 |
| F3.6 | 批量优化指令预设（如：增加冲突、增加心理描写、加快节奏） | P2 |

#### UI 设计

```
┌─────────────────────────────────────────┐
│  最终成品                          [优化修改] │
├─────────────────────────────────────────┤
│  优化要求输入:                           │
│  ┌─────────────────────────────────────┐│
│  │ 1. 增加主角内心独白描写              ││
│  │ 2. 加快前三段的节奏                  ││
│  │ 3. 丰富环境细节描写                  ││
│  └─────────────────────────────────────┘│
│  优化范围: ○ 全文  ● 第三章  ○ 指定段落  │
│                                              │
│           [取消]        [开始优化]           │
└─────────────────────────────────────────┘
```

---

### 2.4 作品管理系统

#### 需求描述
**目标**: 结构化管理每次生成的作品，保存完整创作数据

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F4.1 | 作品列表页，展示所有历史作品 | P0 |
| F4.2 | 作品字段详情: | P0 |
| | - 作品ID (uuid) | |
| | - 标题 title | |
| | - 主题 topic | |
| | - 关键词 keywords | |
| | - 内容类型 type (article/novel) | |
| | - 期望字数 expectedWordCount | |
| | - 实际字数 actualWordCount | |
| | - 章节数 chapterCount | |
| | - 创建时间 createdAt | |
| | - 更新时间 updatedAt | |
| F4.3 | 阶段输入数据: | P0 |
| | - 阶段一输入 params (包含七大参数完整JSON) | |
| | - 阶段一输出 stage1Output | |
| | - 阶段二输入 stage2Input (基于阶段一) | |
| | - 阶段二输出 stage2Output | |
| | - 阶段三输入 stage3Input (基于阶段二) | |
| | - 阶段三输出 stage3Output (最终成品) | |
| F4.4 | 章节详细内容: 每章独立存储 | P1 |
| F4.5 | 作品支持重命名、删除、导出 | P1 |
| F4.6 | 作品搜索/筛选功能 | P2 |
| F4.7 | 作品复制功能（基于现有作品生成新变体） | P2 |

#### 数据模型

```typescript
// 作品基础信息
interface Work {
  id: string;
  title: string;
  topic: string;
  keywords: string;
  type: ContentType;
  expectedWordCount: number;
  actualWordCount: number;
  chapterCount: number;
  status: 'drafting' | 'completed' | 'archived';
  createdAt: number;
  updatedAt: number;
}

// 阶段输入输出
interface StageData {
  stage: 1 | 2 | 3;
  input: GenerationParams;  // 完整入参
  output: string;           // 阶段输出
  cycleResults: CycleResult[];
}

// 章节数据
interface Chapter {
  id: string;
  chapterNumber: number;
  title: string;
  content: string;
  wordCount: number;
  stageData: StageData;  // 该章节的完整生成数据
}
```

---

### 2.5 分镜提示词生成功能

#### 需求描述
**目标**: 基于已生成的作品内容，生成AI视频分镜提示词

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F5.1 | 作品详情页增加"生成分镜"按钮 | P0 |
| F5.2 | 分镜生成入参: | P0 |
| | - 每集时长 duration (默认60s/90s/120s可选) | |
| | - 每集分镜数 clipsPerEpisode (默认6/8/10可选) | |
| | - 每分镜时长 clipDuration | |
| | - 分镜风格 style (写实/动漫/水墨/3D等) | |
| F5.3 | 主题/角色定义: | P0 |
| | - 主角形象描述 (默认从作品提取) | |
| | - 配角形象描述 | |
| | - 场景设定 | |
| | - 色调风格 | |
| | - 上述字段支持AI优化（基于作品内容生成更丰富描述） | |
| F5.4 | 视频相关设置: | P1 |
| | - 分辨率 resolution (1920x1080 / 1280x720 / 竖屏9:16) | |
| | - 画面比例 aspectRatio (16:9 / 9:16 / 1:1) | |
| | - 帧率 fps (24/30/60) | |
| | - 视频格式 format (mp4/mov/webm) | |
| F5.5 | 分镜输出格式: | P0 |
| | - 镜头号 | |
| | - 画面描述 (Prompt) | |
| | - 时长 (秒) | |
| | - 运镜方式 | |
| | - 音效/音乐建议 | |
| F5.6 | 分镜预览功能（可选） | P2 |
| F5.7 | 导出分镜为JSON/Markdown格式 | P1 |

#### 分镜数据结构

```typescript
interface StoryboardConfig {
  // 基础设置
  duration: number;           // 单集时长(秒)
  clipsPerEpisode: number;    // 每集分镜数
  clipDuration: number;       // 单个分镜时长(秒)
  
  // 角色设定
  mainCharacter: CharacterDesc;
  supportingCharacters: CharacterDesc[];
  
  // 场景设定
  scenes: SceneDesc[];
  
  // 视频参数
  resolution: '1920x1080' | '1280x720' | '720x1280';
  aspectRatio: '16:9' | '9:16' | '1:1';
  fps: 24 | 30 | 60;
  
  // 风格
  style: 'realistic' | 'anime' | 'ink' | '3d' | 'cartoon';
  tone: string;
}

interface StoryboardPrompt {
  clipNumber: number;
  scene: string;
  visual: string;          // 画面描述
  duration: number;        // 时长(秒)
  camera: string;           // 运镜方式
  audio: string;            // 音效建议
  lighting: string;        // 光线建议
}
```

---

### 2.6 模型配置管理升级

#### 需求描述
**目标**: 支持单个阶段配置多个模型，运行时不启用

#### 详细需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|--------|
| F6.1 | 新增模型配置管理页面 | P0 |
| F6.2 | 每个阶段支持添加多个模型配置 | P0 |
| F6.3 | 每个阶段支持设置当前启用的模型 | P0 |
| F6.4 | 支持模型配置导入/导出 | P1 |
| F6.5 | 支持模型配置分组/命名 | P2 |
| F6.6 | 显示模型调用统计（可选） | P2 |

#### UI 设计

```
┌─────────────────────────────────────────────────────┐
│  模型配置管理                                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│  阶段 1 - 大纲/骨架                                   │
│  ┌─────────────────────────────────────────────────┐│
│  │ [●] 模型A - volcengine/ark-code-latest        ││
│  │ [ ] 模型B - qwen2.5:14b                       ││
│  │ [ ] 模型C - deepseek-chat                      ││
│  │ [+ 添加模型]                                    ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  阶段 2 - 血肉填充                                   │
│  ┌─────────────────────────────────────────────────┐│
│  │ [●] 模型A - volcengine/ark-code-latest        ││
│  │ [+ 添加模型]                                    ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  阶段 3 - 最终打磨                                    │
│  ┌─────────────────────────────────────────────────┐│
│  │ [●] 模型A - volcengine/ark-code-latest        ││
│  │ [+ 添加模型]                                    ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│              [保存配置]  [导入]  [导出]               │
└─────────────────────────────────────────────────────┘
```

---

## 三、迭代升级计划

### Phase 1: 核心功能重构 (预计 2-3 周)

| 序号 | 任务 | 预估工时 |
|-----|------|---------|
| 1.1 | 数据模型重构 - 新增作品/章节/阶段数据结构 | 2天 |
| 1.2 | 全书生成流程设计 - 大纲→分章→整合 | 3天 |
| 1.3 | 章节生成引擎 - 智能分段+续写 | 3天 |
| 1.4 | 作品管理模块 - 列表/详情/CRUD | 2天 |

### Phase 2: 优化与分镜功能 (预计 2 周)

| 序号 | 任务 | 预估工时 |
|-----|------|---------|
| 2.1 | 修改优化功能 - 弹窗+调用逻辑 | 2天 |
| 2.2 | 分镜提示词生成模块 | 3天 |
| 2.3 | 分镜参数配置页面 | 1天 |
| 2.4 | 视频参数设置 | 1天 |

### Phase 3: 模型配置升级 (预计 1 周)

| 序号 | 任务 | 预估工时 |
|-----|------|---------|
| 3.1 | 模型配置管理页面重构 | 2天 |
| 3.2 | 多模型切换逻辑 | 2天 |
| 3.3 | 配置导入/导出功能 | 1天 |

### Phase 4: UI/UX 优化 (持续)

| 序号 | 任务 | 预估工时 |
|-----|------|---------|
| 4.1 | 响应式布局优化 | 1天 |
| 4.2 | 加载/进度状态优化 | 1天 |
| 4.3 | 错误处理和边界情况 | 1天 |

---

## 四、技术债务与注意事项

### 4.1 需要注意的技术点

1. **状态管理**: 现有Zustand store需要大幅扩展，建议按模块拆分
2. **API调用**: 分段生成需要处理好请求取消和续接逻辑
3. **本地存储**: 作品数据可能较大，需考虑IndexedDB或分片存储
4. **性能**: 长文本渲染需要使用虚拟滚动或分页

### 4.2 潜在的挑战

1. 长文本生成的一致性和连贯性
2. 多章节并行生成 vs 串行生成的性能权衡
3. 分镜提示词生成的语义准确性
4. 模型输出限制的智能截断算法

---

## 五、附录

### 5.1 参考资源

- 火山引擎 ARK API: https://ark.cn-beijing.volces.com/api/coding/v3
- 当前 API Key: ed861344-3bd8-4bc8-8834-9e53d06e82cc

### 5.2 现有组件列表

| 组件名 | 路径 | 功能 |
|-------|------|------|
| InputPanel | src/components/InputPanel.tsx | 参数输入面板 |
| ConfigPanel | src/components/ConfigPanel.tsx | 模型配置面板 |
| OutputPanels | src/components/OutputPanels.tsx | 阶段输出展示 |
| FinalOutput | src/components/FinalOutput.tsx | 最终成品展示 |
| GenerateButton | src/components/GenerateButton.tsx | 生成按钮 |
| CycleProgress | src/components/CycleProgress.tsx | 循环进度显示 |
| HistorySidebar | src/components/HistorySidebar.tsx | 历史记录侧边栏 |

### 5.3 现有类型定义

详见 `src/types/index.ts`

---

**文档结束**

如需进一步细化某个模块的实现方案，请告知。
