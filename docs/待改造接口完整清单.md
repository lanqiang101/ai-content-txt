# 待改造接口完整清单

扫描项目所有数据读写接口，完整清单如下：

## 1. 配置相关接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `setConfig` | 更新流水线配置 | localStorage (Zustand persist) | `configService.savePipelineConfig` - 更新SQLite | ✅ 已完成 |
| `setParams` | 更新生成参数 | localStorage (Zustand persist) | `configService.saveGenerationParams` - 更新SQLite | ✅ 已完成 |
| `toggleDarkMode` | 切换深色模式 | localStorage (Zustand persist) | `configService.setDarkModeConfig` - 更新SQLite | ✅ 已完成 |
| `setTimerAutomation` | 更新定时器自动化配置 | localStorage (Zustand persist) | `configService.saveTimerAutomationConfig` - 更新SQLite | ✅ 已完成 |

## 2. 生成历史接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `addToHistory` | 添加生成历史记录 | 内存 + localStorage | `historyService.createHistory` - 插入SQLite | ✅ 已完成 |
| `clearHistory` | 清空所有历史 | 内存 + localStorage | `historyService.clearAllHistory` - 软删除SQLite所有记录 | ✅ 已完成 |
| `deleteFromHistory` | 删除单条历史 | 内存 + localStorage | `historyService.deleteHistory` - 软删除SQLite记录 | ✅ 已完成 |
| `loadFromHistory` | 从历史加载参数 | 内存 | 无变化（仅内存读取） | ✅ 已完成 |

## 3. 作品管理接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `setWorks` | 设置全部作品列表 | 内存 + localStorage | `workService.setAllWorks` - 重置SQLite所有作品 | ✅ 已完成 |
| `addWork` | 添加新作品 | 内存 + localStorage | `workService.createWork` - 插入SQLite | ✅ 已完成 |
| `updateWork` | 更新作品信息 | 内存 + localStorage | `workService.updateWork` - 更新SQLite | ✅ 已完成 |
| `deleteWork` | 删除作品 | 内存 + localStorage | `workService.deleteWork` - 软删除SQLite | ✅ 已完成 |
| `setCurrentWork` | 设置当前作品ID | 内存状态 | 无变化 | ✅ 已完成 |

## 4. 小说大纲接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `setBookOutline` | 保存/更新小说大纲 | 内存 + localStorage | `outlineService.saveBookOutline` - 保存SQLite | ✅ 已完成 |

## 5. 分镜管理接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `addStoryboard` | 添加分镜结果 | 内存 + localStorage | `storyboardService.createStoryboard` - 插入SQLite | ✅ 已完成 |

## 6. 角色管理接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `addCharacter` | 添加角色 | 内存 + localStorage | `characterService.createCharacter` - 插入SQLite | ✅ 已完成 |
| `updateCharacter` | 更新角色 | 内存 + localStorage | `characterService.updateCharacter` - 更新SQLite | ✅ 已完成 |
| `deleteCharacter` | 删除角色 | 内存 + localStorage | `characterService.deleteCharacter` - 软删除SQLite | ✅ 已完成 |

## 7. 生成状态接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `setGeneration` | 更新生成状态 | 内存 | 无变化（运行时状态） | ✅ 已完成（无需改造） |
| `resetGeneration` | 重置生成状态 | 内存 | 无变化（运行时状态） | ✅ 已完成（无需改造） |

## 8. UI状态接口

| 接口名称 | 功能用途 | 原数据来源 | 改造后数据库操作 | 改造状态 |
|---------|---------|-----------|----------------|----------|
| `toggleConfig` | 切换配置面板显示 | 内存 | 无变化（UI状态） | ✅ 已完成（无需改造） |
| `toggleHistory` | 切换历史侧边栏 | 内存 | 无变化（UI状态） | ✅ 已完成（无需改造） |
| `toggleWorks` | 切换作品侧边栏 | 内存 | 无变化（UI状态） | ✅ 已完成（无需改造） |

## 统计

- 总计接口数量：**27** 个
- 需要数据库改造：**19** 个
- 无需改造（运行时/UI状态）：**8** 个
- 已完成改造：**19** 个
- 未改造：**0** 个

## 改造原则确认

✅ **严格保留原有接口格式**：所有接口名称、调用方式、入参出参完全不变
✅ **仅修改底层存储逻辑**：原有代码调用不需要任何修改
✅ **保持降级兼容**：Zustand persist 仍然保留，作为内存降级
